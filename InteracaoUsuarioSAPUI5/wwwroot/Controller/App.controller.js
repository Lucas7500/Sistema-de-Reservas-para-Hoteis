sap.ui.define([
   "./BaseController"
], (BaseController) => {
   "use strict";

   const CAMINHO_ROTA_APP = "reservas.hoteis.controller.App";
   return BaseController.extend(CAMINHO_ROTA_APP, {});
});